name: CI

on:
  push:
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    container:
      image: ghcr.io/its-me/arch-linux:builder

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: CI
        uses: its-me/actions.aur.ci@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
